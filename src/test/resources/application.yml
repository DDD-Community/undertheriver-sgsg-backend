spring:
  profiles:
    include: auth
  h2:
    console:
      enabled: true
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    hibernate:
      ddl-auto: create-drop

const:
  folder:
    limit: 20

paging:
  folder_config:
    limit: 20
    max_limit: 50
    min_page: 1
  memo_config:
    limit: 20
    max_limit: 50
    min_page: 1
