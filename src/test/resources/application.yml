spring:
  profiles:
    include: auth
  h2:
    console:
      enabled: true
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    hibernate:
      ddl-auto: create-drop

const:
  folder:
    limit: 20

paging:
  folder:
    limit: 20
  memo:
    limit: 20
  max_row: 50
  min_page: 1
